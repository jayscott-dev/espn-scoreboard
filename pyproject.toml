
[project]
name = "espn-scraper"
version = "0.1.0"
description = "ESPN ingestion and processing service"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "fastapi[standard]>=0.138.1",
    "pydantic-settings>=2.14.2",
    "requests>=2.33",
]
